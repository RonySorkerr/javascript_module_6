const nums = [4,5,6,9,6,3,1,4,1,2,3,4,5,6];

console.log(nums);

nums.reverse();
const reversed = nums.reverse(); //same thing in different ways

console.log(reversed);
// console.log(nums); // we will get the same output as the before console.